steps:
  - id: build
    name: gcr.io/cloud-builders/docker
    args:
      - build
      - -t
      - $LOCATION-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/services/landing
      - '.'
  - id: push
    name: 'gcr.io/cloud-builders/docker'
    args:
      - push
      - $LOCATION-docker.pkg.dev/$PROJECT_ID/${_REPOSITORY}/services/landing

substitutions:
  _REPOSITORY: 'infra'